#this is NOT a script
#in bash use:
erl -pa deps/*/ebin -pa ebin/
#this makes erlang look in those dirs for beam files
#then at the erlang shell use:
application:ensure_all_started(swarm).
#which starts cowboy and everything
#then you can use
swarm_server:start()
#now it's running
#in client/stupid.html is a barebones javascript socket.

